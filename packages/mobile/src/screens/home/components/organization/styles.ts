import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  imgContainer: {
    marginTop: 20,
    maxHeight: 50,
    width: '30%',
    alignItems: 'center',
  },
  image: {
    maxWidth: '100%',
    maxHeight: '100%',
    resizeMode: 'contain',
  },
});

export { styles };
