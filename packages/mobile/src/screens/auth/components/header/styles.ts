import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: {
    paddingTop: 24,
    height: 150,
  },
  headerText: {
    lineHeight: 22,
  },
  logo: {
    width: 180,
    height: 45,
    resizeMode: 'contain',
  },
});

export { styles };
