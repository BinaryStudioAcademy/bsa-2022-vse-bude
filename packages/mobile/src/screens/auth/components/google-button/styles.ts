import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonWrapper: {
    width: '100%',
    borderWidth: 2,
    borderRadius: 10,
    paddingVertical: 13,
    marginTop: 25,
  },
  buttonText: {
    lineHeight: 19,
    textAlign: 'center',
  },
});

export { styles };
