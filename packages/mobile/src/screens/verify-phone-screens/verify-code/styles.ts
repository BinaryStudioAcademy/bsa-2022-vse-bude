import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: {
    width: 170,
  },
  icon: {
    position: 'absolute',
    bottom: 14,
    right: 14,
  },
});

export { styles };
