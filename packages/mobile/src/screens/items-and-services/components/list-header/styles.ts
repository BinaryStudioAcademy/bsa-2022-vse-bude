import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  filterWrapper: {
    flexWrap: 'wrap',
  },
  filterContainer: {
    marginTop: 10,
    marginRight: 10,
  },
});

export { styles };
