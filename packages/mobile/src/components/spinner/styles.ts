import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  overflowContainer: {
    position: 'absolute',
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    elevation: 1,
    zIndex: 1,
  },
});

export { styles };
