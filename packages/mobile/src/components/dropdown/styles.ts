import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 15,
  },
  label: {
    lineHeight: 14,
  },
  required: {
    marginTop: -4,
  },
  dropDown: {
    borderWidth: 0,
    paddingHorizontal: 15,
    paddingVertical: 0,
    borderRadius: 10,
  },
});

export { styles };
