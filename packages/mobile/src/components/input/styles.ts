import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    paddingVertical: 6,
    paddingHorizontal: 15,
    borderRadius: 10,
    lineHeight: 16,
    borderWidth: 2,
  },
  label: {
    lineHeight: 14,
  },
});

export { styles };
