import { StyleSheet } from 'react-native';
import { FontFamily, FontSize } from '~/common/enums/enums';

const styles = StyleSheet.create({
  default: {
    fontFamily: FontFamily.RALEWAY_REGULAR,
    fontSize: FontSize.TEXT_16,
  },
});

export { styles };
