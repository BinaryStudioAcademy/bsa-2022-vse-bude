enum DateTimeType {
  DATE = 'date',
  TIME = 'time',
  DATE_TIME = 'datetime',
}

enum DateTimeFormat {
  DATE_ONLY = 'DD/MM/YYYY',
  TIME_ONLY = 'HH:mm',
  DATE_TIME = 'DD/MM/YYYY HH:mm',
}

export { DateTimeType, DateTimeFormat };
