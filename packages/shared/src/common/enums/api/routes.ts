export enum ApiRoutes {
  USERS = '/users',
  AUTH = '/auth',
  UPLOAD_IMAGE = '/upload-image',
}

export enum AuthApiRoutes {
  SIGN_IN = '/sign-in',
  SIGN_UP = '/sign-up',
  SIGN_OUT = '/sign-out',
  REFRESH_TOKEN = '/refresh-token',
  USER = '/user',
}
