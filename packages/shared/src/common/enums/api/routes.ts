export enum ApiRoutes {
  RANDOM_DATA = '/random-data',
  USERS = '/users',
  AUTH = '/auth',
}

export enum AuthApiRoutes {
  SIGN_IN = '/sign-in',
  SIGN_UP = '/sign-up',
  REFRESH_TOKEN = '/refresh-token',
}
