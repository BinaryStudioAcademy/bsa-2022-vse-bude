{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "src",
    "module": "commonjs",
    "paths": {
      "@types": ["common/types"],
      "@enums": ["common/enums"],
      "@helpers": ["helpers"],
      "@models": ["data/models"],
      "@repositories": ["data/repositories"],
      "@services": ["services"],
      "@routes": ["api/routes"],
      "@middlewares": ["api/middlewares"]
    },
    "outDir": "build"
  }
}
